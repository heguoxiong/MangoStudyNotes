#### 20220616

```
# 做之前先想清楚
# 域名特征评级
	# IP特征
	# 1.IP池的变化性
	（在不同时间窗口，IP池变化稳定的域名偏白；恶意域名控制的主机的IP分布往往比较分散）
	怎样算变化的IP？
		需要是不同网段，才能算做变化。
		ip_sum_set = set()
		ip_net = ip.rsplit('.', '1')[0]
	# 2.IP总数>阈值&&存在CNAME？？？待确定。是否有必要做？优先级？怎么做？
	（IP总数较多且存在CNAME解析记录的域名，可能是CDN托管域名，偏白）
		这一批域名置信度高
	# 域名解析次数
	# 1.解析次数的稳定性
	（正常域名在每个时间窗口都会有一定的流量，而恶意域名则在活动期才会被大量解析）
		时间窗口越小越好，因为恶意域名的活动周期短，时间窗口越短越能捕获恶意域名的特征。
		解析600次/每月，8/12。视为较为活跃的企业域名。
		解析600次/每月，6/12。视为中等
			次数阈值怎么定？先理清这个阈值的价值意义是什么。价值是衡量正常企业域名在每月的访问次数。
			看看正常企业域名在pdns上的每月解析次数
			看看位置DNS域名1/4及中位数的解析次数
			看看抽检域名1/4及中位数的解析次数
	# 2.解析次数的方差/n
		为什么要计算解析次数的方差？
		因为有些域名可能只在某个短周期内大量解析，而在其他时段解析较少。这类域名的解析特征极为不稳定。正常域名一般比较稳定。
	# 域名建设
	# 1.是否有自己的www,ns,mx，soa
	# 2.是否存在ns,soa,mx记录
	# 子域名丰富度
	# 1.三级子域名命中白前缀比例（3/5;3 && >5)
	# 域名生命周期
	# 1.whois查询域名创建时间到现在的长度
		# 注意
	# 底线
    # 1.域名||www主站能够ping通
    # 2.不可命中域名服务商
    # 3.不可命中动态域名
    # 4.不可命中黑库域名
    
    
# 其他待做特征
	# 图数据库
	# 词频白名单
# 了解统计数字的中位数，1/4位数
# 了解轮换代理爬虫。首先解决爬虫->根据购买信息获取ip和端口号->轮换代理爬虫
```

